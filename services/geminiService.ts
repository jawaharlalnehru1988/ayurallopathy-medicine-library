// Gemini AI service has been disabled
// The application works perfectly without AI features

export const getMedicineInfo = async (medicineName: string, category: string) => {
  console.log('AI assist feature is disabled');
  return null;
};

export const getDiseaseInfo = async (diseaseName: string) => {
  console.log('AI assist feature is disabled');
  return null;
};
